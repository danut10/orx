<?xml version="1.0" encoding="UTF-8"?>

<beans
	xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd">

	<!--  
	<bean id="appInfo" class="ro.kepler.kepres.web.core.AppInfo">
    	<property name="majorVersion" value="${appInfo.majorVersion}"/>
		<property name="minorVersion" value="${appInfo.minorVersion}"/>
		<property name="release" value="${appInfo.release}"/>
		<property name="stage" value="${appInfo.stage}"/>	
		<property name="environment" value="${appInfo.environment}"/>		
	</bean>	
	
	<bean id="appSettings" class="ro.kepler.kepres.common.dataValues.AppSettings">
		<property name="basedir" value="${appSettings.basedir}"/>
	</bean>	

	<bean id="smtpInfo" class="ro.kepler.kepres.common.dataValues.SmtpInfo">
		<property name="host" value="${smtp.host}"/>
		<property name="port" value="${smtp.port}"/>
		<property name="needsAuth" value="${smtp.needsAuth}"/>
		<property name="username" value="${smtp.username}"/>
		<property name="password" value="${smtp.password}"/>
		<property name="from" value="${smtp.from}"/>
	</bean>		
	
	<bean id="smsInfo" class="ro.kepler.kepres.common.dataValues.SmsInfo">
		<property name="url" value="${sms.url}"/>
		<property name="username" value="${sms.username}"/>
		<property name="password" value="${sms.password}"/>
		<property name="source" value="${sms.source}"/>
	</bean>	
	-->
	
</beans>
